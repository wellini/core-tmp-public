spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  jpa:
    database: h2
    hibernate:
      ddl-auto: none
    show-sql: true
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    password: root
    username: toor
  liquibase:
    change-log: classpath:db.changelog/changelog.xml
    enabled: true

---

spring:
  profiles: test
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:10-alpine:///core_test
    password: postgres
    username: postgres