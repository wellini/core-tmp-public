spring:
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/roadmaps_db?currentSchema=core
    username: roadmaps
    password: roadmaps

springdoc:
  api-docs:
    path: /api/swagger
  swagger-ui:
    path: /api/swagger-ui